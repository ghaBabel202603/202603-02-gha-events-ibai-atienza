name: 02 - Eventos
on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "15 18 2 3 *"
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: ShowTrigger
        run: |
          echo "The event name is: ${{github.event_name}}"
